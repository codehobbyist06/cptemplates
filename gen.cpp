#include<bits/stdc++.h>
using namespace std;

#define test_lim 10
#define size_lim 10

int main(int argc, char* argv[]){
	if(argv[1] != NULL){
	    srand(atoi(argv[1]));
	}
	vector<int> v;
	long long t = rand() % test_lim;
	long long n = rand() % size_lim;
	return 0;
}